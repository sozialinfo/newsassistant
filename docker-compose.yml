networks:
  opencode:
    external: true

services:
  crawl4ai:
    image: unclecode/crawl4ai:latest
    container_name: crawl4ai-newsassistant
    restart: unless-stopped
    shm_size: 1g
    networks:
      - opencode

  odoo:
    image: odoo:18.0
    container_name: odoo-newsassistant
    restart: unless-stopped
    depends_on: []
    environment:
      - HOST=postgres
      - PORT=5432
      - USER=opencode
      - PASSWORD=${POSTGRES_PASSWORD}
      - INFOMANIAK_AI_API_KEY=${INFOMANIAK_AI_API_KEY}
    volumes:
      - .:/mnt/extra-addons
      - ./odoo.conf:/etc/odoo/odoo.conf:ro
      - odoo-data:/var/lib/odoo
      - /home/debian/shared/odoo-src/18.0/oca/web:/mnt/oca/web:ro
      - /home/debian/shared/odoo-src/18.0/oca/queue:/mnt/oca/queue:ro
    networks:
      - opencode

volumes:
  odoo-data:
