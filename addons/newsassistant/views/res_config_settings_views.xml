<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_newsassistant" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.newsassistant</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="News Assistant" string="News Assistant"
                     name="newsassistant"
                     groups="newsassistant.newsassistant_group_admin">
                    <block title="Article Defaults">
                        <setting string="Default Stage for New Articles"
                                 help="Stage assigned when a new article is created. Defaults to 'New' if not set.">
                            <field name="newsassistant_new_article_stage_id"
                                   options="{'no_create': True}"/>
                        </setting>
                    </block>
                    <block title="AI Configuration">
                        <setting string="Infomaniak AI Product ID"
                                 help="Infomaniak AI product ID used for all AI API calls. Default: 103794.">
                            <field name="newsassistant_infomaniak_product_id"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_newsassistant_settings" model="ir.actions.act_window">
        <field name="name">News Assistant Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'newsassistant'}</field>
    </record>
</odoo>
